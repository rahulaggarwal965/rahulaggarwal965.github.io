[project]
name = "website"
version = "0.1.0"
description = "Personal website — static site builder."
requires-python = ">=3.10"
dependencies = [
    "markdown>=3.5",
    "pymdown-extensions>=10.0",
]
